"""
Script for registering and saving model versions.
"""

def register_models(full_model: str, distilled_model: str, quantized_model: str) -> None:
    """
    TODO: Register all model versions in the model registry and mark quantized model as production.
    
    Args:
        full_model (str): Path to the full trained model
        distilled_model (str): Path to the distilled model
        quantized_model (str): Path to the quantized model
    """
    pass
